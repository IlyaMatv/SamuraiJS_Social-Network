let initialState = {
    friends: [
        {name: 'Ilya'},
        {name: 'Stomka'},
        {name: 'Alexey'}
    ]
}

const sitebarReducer = (state = initialState, action) => {
    
    return state
}

export default sitebarReducer